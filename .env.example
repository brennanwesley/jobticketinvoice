# JobTicketInvoice Backend Configuration Template
# Copy this file to .env and update with your actual values

# ========================================
# AUTHENTICATION & SECURITY
# ========================================

# JWT Secret Key - MUST be at least 32 characters for security
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-very-secure-secret-key-here-minimum-32-characters
JWT_SECRET_KEY=your-very-secure-secret-key-here-minimum-32-characters

# JWT Configuration
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Fernet Encryption Key for field-level encryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=your-fernet-encryption-key-here

# CORS Configuration
SECURITY_CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","https://yourdomain.com"]
CORS_ALLOW_CREDENTIALS=true

# ========================================
# DATABASE
# ========================================

# Database URL - SQLite for development, PostgreSQL for production
DATABASE_URL=sqlite:///./jobticket.db
# DATABASE_URL=postgresql://user:password@localhost:5432/jobticket
DATABASE_ECHO=false

# ========================================
# EMAIL CONFIGURATION
# ========================================

# Email Development Mode (logs emails instead of sending)
EMAIL_DEV_MODE=true

# SendGrid Configuration (Production)
# EMAIL_SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
# EMAIL_SENDGRID_FROM_EMAIL=noreply@yourdomain.com

# SMTP Configuration (Alternative to SendGrid)
# EMAIL_SMTP_SERVER=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_SMTP_USERNAME=your-email@gmail.com
# EMAIL_SMTP_PASSWORD=your-app-password

# Email Settings
EMAIL_FROM_EMAIL=noreply@jobticketinvoice.com
EMAIL_FROM_NAME=JobTicket Invoice
EMAIL_FRONTEND_URL=http://localhost:3000

# ========================================
# APPLICATION SETTINGS
# ========================================

# Environment: development, staging, production, testing
ENVIRONMENT=development

# Debug mode (automatically disabled in production)
DEBUG=true

# API Configuration
API_V1_PREFIX=/api/v1
PROJECT_NAME=JobTicket Invoice API

# Frontend URL for email links and CORS
FRONTEND_URL=http://localhost:3000

# File Upload Settings (in bytes)
MAX_UPLOAD_SIZE=10485760  # 10MB

# ========================================
# FEATURE FLAGS
# ========================================

# Dangerous operations (automatically disabled in production)
ALLOW_DATABASE_RESET=false
ALLOW_DATA_DELETION=false

# Monitoring and Logging
ENABLE_REQUEST_LOGGING=true
ENABLE_PERFORMANCE_MONITORING=false

# ========================================
# PRODUCTION NOTES
# ========================================

# For production deployment:
# 1. Set ENVIRONMENT=production
# 2. Use strong, unique SECRET_KEY and FERNET_KEY
# 3. Configure proper database (PostgreSQL recommended)
# 4. Set up email service (SendGrid or SMTP)
# 5. Configure proper CORS origins (HTTPS only)
# 6. Disable DEBUG mode
# 7. Review and disable development feature flags
